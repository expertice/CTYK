# Copy to .env.local and adjust.

# Optional: only for one-time `pnpm models:pyannote-offline` or for `hf_pyannote` / Hub downloads.
# Not required at app runtime if you use the offline bundle under .models/diarization/offline-bundle/
# HF_TOKEN=

# Optional overrides for local pyannote (see LOCAL_MODELS.md)
# DIARIZATION_LOCAL_MODEL_PATH=.models/diarization/offline-bundle/pipeline
# PYANNOTE_OFFLINE_PIPELINE=
# FFMPEG_BIN=D:\projects\CTYK\apps\web\.tools\ffmpeg\ffmpeg-8.1-essentials_build\bin\ffmpeg.exe
